package murat.simv2.simulation;

import java.lang.reflect.Array;
import java.lang.reflect.Constructor;
import java.lang.reflect.Field;
import java.lang.reflect.Method;
import java.lang.reflect.Modifier;
import java.util.ArrayList;
import net.minecraft.client.network.ClientPlayerEntity;

// Generated by WALA movement field analysis — do not edit
public final class GeneratedSync {
    private GeneratedSync() {
    }

    public static void sync(ClientPlayerEntity real, murat.simv2.simulation.mirror.net.minecraft.client.network.ClientPlayerEntity runtimeSim) {
        // ── ClientPlayerEntity ──
        setFieldIfPresent(runtimeSim, "activeHand", copyValue(real.activeHand, getFieldIfPresent(runtimeSim, "activeHand"), true));
        setFieldIfPresent(runtimeSim, "autoJumpEnabled", real.autoJumpEnabled);
        setFieldIfPresent(runtimeSim, "falling", real.falling);
        setFieldIfPresent(runtimeSim, "field_3938", real.field_3938);
        setFieldIfPresent(runtimeSim, "inSneakingPose", real.inSneakingPose);
        setFieldIfPresent(runtimeSim, "input", copyValue(real.input, getFieldIfPresent(runtimeSim, "input"), true));
        setFieldIfPresent(runtimeSim, "lastNauseaIntensity", real.lastNauseaIntensity);
        setFieldIfPresent(runtimeSim, "mountJumpStrength", real.mountJumpStrength);
        setFieldIfPresent(runtimeSim, "nauseaIntensity", real.nauseaIntensity);
        setFieldIfPresent(runtimeSim, "ticksLeftToDoubleTapSprint", real.ticksLeftToDoubleTapSprint);
        setFieldIfPresent(runtimeSim, "ticksToNextAutoJump", real.ticksToNextAutoJump);
        setFieldIfPresent(runtimeSim, "underwaterVisibilityTicks", real.underwaterVisibilityTicks);
        setFieldIfPresent(runtimeSim, "usingItem", real.usingItem);
        // ── Entity ──
        setFieldIfPresent(runtimeSim, "blockPos", copyValue(real.blockPos, getFieldIfPresent(runtimeSim, "blockPos"), true));
        setFieldIfPresent(runtimeSim, "boundingBox", copyValue(real.boundingBox, getFieldIfPresent(runtimeSim, "boundingBox"), true));
        setFieldIfPresent(runtimeSim, "chunkPos", copyValue(real.chunkPos, getFieldIfPresent(runtimeSim, "chunkPos"), true));
        setFieldIfPresent(runtimeSim, "collidedSoftly", real.collidedSoftly);
        setFieldIfPresent(runtimeSim, "commandTags", copyValue(real.commandTags, getFieldIfPresent(runtimeSim, "commandTags"), true));
        setFieldIfPresent(runtimeSim, "customData", copyValue(real.customData, getFieldIfPresent(runtimeSim, "customData"), true));
        setFieldIfPresent(runtimeSim, "dimensions", copyValue(real.dimensions, getFieldIfPresent(runtimeSim, "dimensions"), true));
        setFieldIfPresent(runtimeSim, "fallDistance", real.fallDistance);
        setFieldIfPresent(runtimeSim, "fireTicks", real.fireTicks);
        setFieldIfPresent(runtimeSim, "fluidHeight", copyValue(real.fluidHeight, getFieldIfPresent(runtimeSim, "fluidHeight"), true));
        setFieldIfPresent(runtimeSim, "forceUpdateSupportingBlockPos", real.forceUpdateSupportingBlockPos);
        setFieldIfPresent(runtimeSim, "groundCollision", real.groundCollision);
        setFieldIfPresent(runtimeSim, "horizontalCollision", real.horizontalCollision);
        setFieldIfPresent(runtimeSim, "invulnerable", real.invulnerable);
        setFieldIfPresent(runtimeSim, "movementMultiplier", copyValue(real.movementMultiplier, getFieldIfPresent(runtimeSim, "movementMultiplier"), true));
        setFieldIfPresent(runtimeSim, "noClip", real.noClip);
        setFieldIfPresent(runtimeSim, "onGround", real.onGround);
        setFieldIfPresent(runtimeSim, "pistonMovementDelta", copyValue(real.pistonMovementDelta, getFieldIfPresent(runtimeSim, "pistonMovementDelta"), true));
        setFieldIfPresent(runtimeSim, "pistonMovementTick", real.pistonMovementTick);
        setFieldIfPresent(runtimeSim, "pitch", real.pitch);
        setFieldIfPresent(runtimeSim, "portalCooldown", real.portalCooldown);
        setFieldIfPresent(runtimeSim, "pos", copyValue(real.pos, getFieldIfPresent(runtimeSim, "pos"), true));
        setFieldIfPresent(runtimeSim, "speed", real.speed);
        setFieldIfPresent(runtimeSim, "standingEyeHeight", real.standingEyeHeight);
        setFieldIfPresent(runtimeSim, "stateAtPos", copyValue(real.stateAtPos, getFieldIfPresent(runtimeSim, "stateAtPos"), true));
        setFieldIfPresent(runtimeSim, "submergedFluidTag", copyValue(real.submergedFluidTag, getFieldIfPresent(runtimeSim, "submergedFluidTag"), true));
        setFieldIfPresent(runtimeSim, "supportingBlockPos", copyValue(real.supportingBlockPos, getFieldIfPresent(runtimeSim, "supportingBlockPos"), true));
        setFieldIfPresent(runtimeSim, "touchingWater", real.touchingWater);
        setFieldIfPresent(runtimeSim, "trackedPosition", copyValue(real.trackedPosition, getFieldIfPresent(runtimeSim, "trackedPosition"), true));
        setFieldIfPresent(runtimeSim, "vehicle", copyValue(real.vehicle, getFieldIfPresent(runtimeSim, "vehicle"), true));
        setFieldIfPresent(runtimeSim, "velocity", copyValue(real.velocity, getFieldIfPresent(runtimeSim, "velocity"), true));
        setFieldIfPresent(runtimeSim, "velocityModified", real.velocityModified);
        setFieldIfPresent(runtimeSim, "verticalCollision", real.verticalCollision);
        setFieldIfPresent(runtimeSim, "yaw", real.yaw);
        // ── LivingEntity ──
        setFieldIfPresent(runtimeSim, "activeItemStack", copyValue(real.activeItemStack, getFieldIfPresent(runtimeSim, "activeItemStack"), true));
        setFieldIfPresent(runtimeSim, "activeStatusEffects", copyValue(real.activeStatusEffects, getFieldIfPresent(runtimeSim, "activeStatusEffects"), true));
        setFieldIfPresent(runtimeSim, "attributes", copyValue(real.attributes, getFieldIfPresent(runtimeSim, "attributes"), true));
        setFieldIfPresent(runtimeSim, "climbingPos", copyValue(real.climbingPos, getFieldIfPresent(runtimeSim, "climbingPos"), true));
        setFieldIfPresent(runtimeSim, "equipment", copyValue(real.equipment, getFieldIfPresent(runtimeSim, "equipment"), true));
        setFieldIfPresent(runtimeSim, "forwardSpeed", real.forwardSpeed);
        setFieldIfPresent(runtimeSim, "glidingTicks", real.glidingTicks);
        setFieldIfPresent(runtimeSim, "headYaw", real.headYaw);
        setFieldIfPresent(runtimeSim, "itemUseTimeLeft", real.itemUseTimeLeft);
        setFieldIfPresent(runtimeSim, "jumping", real.jumping);
        setFieldIfPresent(runtimeSim, "jumpingCooldown", real.jumpingCooldown);
        setFieldIfPresent(runtimeSim, "movementSpeed", real.movementSpeed);
        setFieldIfPresent(runtimeSim, "noDrag", real.noDrag);
        setFieldIfPresent(runtimeSim, "riptideTicks", real.riptideTicks);
        setFieldIfPresent(runtimeSim, "sidewaysSpeed", real.sidewaysSpeed);
        setFieldIfPresent(runtimeSim, "upwardSpeed", real.upwardSpeed);
        // ── PlayerEntity ──
        setFieldIfPresent(runtimeSim, "abilities", copyValue(real.abilities, getFieldIfPresent(runtimeSim, "abilities"), true));
        setFieldIfPresent(runtimeSim, "abilityResyncCountdown", real.abilityResyncCountdown);
        setFieldIfPresent(runtimeSim, "hungerManager", copyValue(real.hungerManager, getFieldIfPresent(runtimeSim, "hungerManager"), true));
        setFieldIfPresent(runtimeSim, "inventory", copyValue(real.inventory, getFieldIfPresent(runtimeSim, "inventory"), true));
        setFieldIfPresent(runtimeSim, "lastStrideDistance", real.lastStrideDistance);
        setFieldIfPresent(runtimeSim, "strideDistance", real.strideDistance);
    }


    @SuppressWarnings("unchecked")
    private static <T> T copyValue(T realValue, T simValue, boolean deepCopy) {
        if (!deepCopy) {
            return realValue;
        }
        try {
            return (T) syncValue(realValue, simValue, new ArrayList<>());
        } catch (ReflectiveOperationException | java.lang.reflect.InaccessibleObjectException ex) {
            // Fallback to the real instance when deep reflection is blocked.
            // This favors simulation continuity over hard crashes.
            return realValue;
        }
    }

    private static Object syncValue(Object realValue, Object simValue, ArrayList<SeenPair> seen) throws ReflectiveOperationException {
        if (realValue == null) {
            return null;
        }
        if (simValue == realValue) {
            return realValue;
        }
        if (isSimpleValueType(realValue.getClass())) {
            return cloneSimpleValue(realValue);
        }
        for (SeenPair pair : seen) {
            if (pair.matches(realValue, simValue)) {
                return simValue != null ? simValue : realValue;
            }
        }
        seen.add(new SeenPair(realValue, simValue));

        if (realValue.getClass().isArray()) {
            return syncArray(realValue, simValue, seen);
        }
        if (realValue instanceof java.util.Map<?, ?> realMap) {
            return syncMap(realMap, simValue, seen);
        }
        if (realValue instanceof java.util.Collection<?> realCollection) {
            return syncCollection(realCollection, simValue, seen);
        }

        Object target = simValue;
        if (target != null && !realValue.getClass().isInstance(target)) {
            target = null;
        }
        if (target == null) {
            Object cloned = tryCloneObject(realValue);
            if (cloned != null) {
                return cloned;
            }
            target = instantiateLike(realValue.getClass());
            if (target == null) {
                return realValue;
            }
        }

        deepSyncObject(realValue, target, seen);
        return target;
    }

    private static void deepSyncObject(Object realValue, Object simValue, ArrayList<SeenPair> seen) throws ReflectiveOperationException {
        Class<?> current = realValue.getClass();
        while (current != null && current != Object.class) {
            for (Field field : current.getDeclaredFields()) {
                if (Modifier.isStatic(field.getModifiers()) || field.isSynthetic()) {
                    continue;
                }
                try {
                    field.setAccessible(true);
                } catch (java.lang.reflect.InaccessibleObjectException ignored) {
                    continue;
                }
                Object realFieldValue;
                Object simFieldValue;
                try {
                    realFieldValue = field.get(realValue);
                    simFieldValue = field.get(simValue);
                } catch (java.lang.reflect.InaccessibleObjectException ignored) {
                    continue;
                }
                Object copied = syncValue(realFieldValue, simFieldValue, seen);
                if (!Modifier.isFinal(field.getModifiers()) && copied != simFieldValue) {
                    try {
                        field.set(simValue, copied);
                    } catch (java.lang.reflect.InaccessibleObjectException ignored) {
                        // Keep simulation running even if this member cannot be written.
                    }
                }
            }
            current = current.getSuperclass();
        }
    }

    private static Object syncArray(Object realArray, Object simArray, ArrayList<SeenPair> seen) throws ReflectiveOperationException {
        int length = Array.getLength(realArray);
        Class<?> componentType = realArray.getClass().getComponentType();
        Object target = simArray;
        if (target == null || !target.getClass().isArray() || Array.getLength(target) != length) {
            target = Array.newInstance(componentType, length);
        }
        for (int i = 0; i < length; i++) {
            Object realElement = Array.get(realArray, i);
            Object simElement = Array.get(target, i);
            Array.set(target, i, syncValue(realElement, simElement, seen));
        }
        return target;
    }

    @SuppressWarnings({ "rawtypes", "unchecked" })
    private static Object syncMap(java.util.Map<?, ?> realMap, Object simValue, ArrayList<SeenPair> seen) throws ReflectiveOperationException {
        java.util.Map target = (simValue instanceof java.util.Map<?, ?> existing)
            ? (java.util.Map) existing
            : instantiateMap(realMap.getClass());
        if (target == null) {
            target = new java.util.LinkedHashMap();
        }
        try {
            target.clear();
        } catch (UnsupportedOperationException ex) {
            java.util.Map replacement = instantiateMap(realMap.getClass());
            if (replacement == null) {
                replacement = new java.util.LinkedHashMap();
            } else {
                try {
                    replacement.clear();
                } catch (UnsupportedOperationException ignored) {
                    replacement = new java.util.LinkedHashMap();
                }
            }
            target = replacement;
        }
        for (java.util.Map.Entry<?, ?> entry : realMap.entrySet()) {
            Object key = syncValue(entry.getKey(), null, seen);
            Object value = syncValue(entry.getValue(), null, seen);
            target.put(key, value);
        }
        return target;
    }

    @SuppressWarnings({ "rawtypes", "unchecked" })
    private static Object syncCollection(java.util.Collection<?> realCollection, Object simValue, ArrayList<SeenPair> seen) throws ReflectiveOperationException {
        java.util.Collection target = (simValue instanceof java.util.Collection<?> existing)
            ? (java.util.Collection) existing
            : instantiateCollection(realCollection.getClass());
        if (target == null) {
            target = createMutableCollection(realCollection);
        }
        try {
            target.clear();
        } catch (UnsupportedOperationException ex) {
            target = createMutableCollection(realCollection);
        }
        for (Object value : realCollection) {
            target.add(syncValue(value, null, seen));
        }
        return target;
    }

    @SuppressWarnings({ "rawtypes", "unchecked" })
    private static java.util.Collection createMutableCollection(java.util.Collection<?> source) {
        if (source instanceof java.util.Set<?>) {
            return new java.util.LinkedHashSet();
        }
        return new java.util.ArrayList();
    }

    private static Object cloneSimpleValue(Object value) throws ReflectiveOperationException {
        Object cloned = tryCloneObject(value);
        return cloned != null ? cloned : value;
    }

    private static Object tryCloneObject(Object value) throws ReflectiveOperationException {
        if (value == null) {
            return null;
        }
        try {
            Method copy = value.getClass().getDeclaredMethod("copy");
            copy.setAccessible(true);
            return copy.invoke(value);
        } catch (NoSuchMethodException ignored) {
        }
        try {
            Constructor<?> copyCtor = value.getClass().getDeclaredConstructor(value.getClass());
            copyCtor.setAccessible(true);
            return copyCtor.newInstance(value);
        } catch (NoSuchMethodException ignored) {
        }
        return null;
    }

    private static Object instantiateLike(Class<?> type) throws ReflectiveOperationException {
        try {
            Constructor<?> ctor = type.getDeclaredConstructor();
            ctor.setAccessible(true);
            return ctor.newInstance();
        } catch (NoSuchMethodException ignored) {
            return null;
        }
    }

    @SuppressWarnings({ "rawtypes", "unchecked" })
    private static java.util.Map instantiateMap(Class<?> type) throws ReflectiveOperationException {
        Object instance = instantiateLike(type);
        return instance instanceof java.util.Map<?, ?> map ? (java.util.Map) map : null;
    }

    @SuppressWarnings({ "rawtypes", "unchecked" })
    private static java.util.Collection instantiateCollection(Class<?> type) throws ReflectiveOperationException {
        Object instance = instantiateLike(type);
        return instance instanceof java.util.Collection<?> collection ? (java.util.Collection) collection : null;
    }

    private static boolean isSimpleValueType(Class<?> type) {
        return type.isPrimitive()
            || type.isEnum()
            || Number.class.isAssignableFrom(type)
            || Boolean.class == type
            || Character.class == type
            || String.class == type
            || java.util.UUID.class == type
            || type.getName().startsWith("net.minecraft.util.math.Vec")
            || type.getName().startsWith("net.minecraft.util.math.BlockPos")
            || type.getName().startsWith("net.minecraft.util.math.Box")
            || type.getName().startsWith("net.minecraft.text.Text")
            || type.getName().startsWith("java.util.Optional");
    }

    private static Object getFieldIfPresent(Object target, String fieldName) {
        Field field = findField(target.getClass(), fieldName);
        if (field == null) {
            return null;
        }
        try {
            field.setAccessible(true);
            return field.get(target);
        } catch (ReflectiveOperationException | java.lang.reflect.InaccessibleObjectException ignored) {
            return null;
        }
    }

    private static void setFieldIfPresent(Object target, String fieldName, Object value) {
        Field field = findField(target.getClass(), fieldName);
        if (field == null || Modifier.isFinal(field.getModifiers())) {
            return;
        }
        if (value != null && !isAssignableValue(field.getType(), value)) {
            return;
        }
        try {
            field.setAccessible(true);
            field.set(target, value);
        } catch (ReflectiveOperationException | java.lang.reflect.InaccessibleObjectException ignored) {
            // Keep simulation running if this field can't be written.
        }
    }

    private static Field findField(Class<?> type, String fieldName) {
        Class<?> current = type;
        while (current != null && current != Object.class) {
            try {
                return current.getDeclaredField(fieldName);
            } catch (NoSuchFieldException ignored) {
                current = current.getSuperclass();
            }
        }
        return null;
    }

    private static boolean isAssignableValue(Class<?> targetType, Object value) {
        if (value == null) {
            return !targetType.isPrimitive();
        }
        if (!targetType.isPrimitive()) {
            return targetType.isInstance(value);
        }
        return (targetType == boolean.class && value instanceof Boolean)
            || (targetType == byte.class && value instanceof Byte)
            || (targetType == short.class && value instanceof Short)
            || (targetType == int.class && value instanceof Integer)
            || (targetType == long.class && value instanceof Long)
            || (targetType == float.class && value instanceof Float)
            || (targetType == double.class && value instanceof Double)
            || (targetType == char.class && value instanceof Character);
    }

    private record SeenPair(Object realValue, Object simValue) {
        private boolean matches(Object otherReal, Object otherSim) {
            return realValue == otherReal && simValue == otherSim;
        }
    }
}
